import { PoDirective } from './po.directive';

describe('PoDirective', () => {
  it('should create an instance', () => {
    const directive = new PoDirective();
    expect(directive).toBeTruthy();
  });
});
